# Django 训练服务依赖（104 生产 venv 对齐）
# GPU 版 PyTorch 安装见 deploy/setup-new-env.md

Django>=5.2,<6
djangorestframework>=3.16,<4
django-cors-headers>=4.9,<5
django-redis>=6.0,<7
celery>=5.5,<6
redis>=6.4,<7

numpy>=2.0,<3
pandas>=2.3,<3
opencv-python-headless>=4.8,<5
matplotlib>=3.10,<4
PyYAML>=6.0,<7
psutil>=7.0,<8
tqdm>=4.67,<5
joblib>=1.5,<2
thop>=0.1.1
timm>=0.9
Pillow>=11.0,<12
requests>=2.32,<3

# SAM 相关 API（若不用 SAM 可注释）
segment-anything>=1.0

# Django 进程内 YOLO 推理路径会 import torch（104: 2.4.1+cu124）
# 请按 setup-new-env.md 安装带 CUDA 的 torch，勿直接用 pip 默认源覆盖
# torch>=2.4.0
