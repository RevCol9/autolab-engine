# 训练模块环境变量（复制到 /etc/niii/training-algorithm.env 或 systemd Environment）

NIII_TRAINING_STORAGE=/niii_machine_version/AI_trainning_platform/storage
NIII_TRAINING_PLATFORM_ROOT=/niii_machine_version/AI_trainning_platform
NIII_YOLO_PYTHON=/root/.local/share/mamba/envs/yolo/bin/python

# 允许 API 加载预训练模型的根目录；多个目录使用系统 PATH 分隔符（Linux 为 :）
# 未配置时默认仅允许 NIII_TRAINING_PLATFORM_ROOT
# NIII_TRAINING_MODEL_ROOTS=/niii_machine_version/AI_trainning_platform:/home/model

# 可选：按路由分别覆盖任务配置。旧 TRAINING_CONFIG_PATH 仍可用，
# 但对应 YAML 必须声明 train_task，避免检测/分割配置串用。
# TRAINING_DETECTION_CONFIG_PATH=/etc/niii/training-detection.yaml
# TRAINING_SEGMENTATION_CONFIG_PATH=/etc/niii/training-segmentation.yaml

# 训练 HTTP
AUTOLAB_TRAIN_HOST=0.0.0.0
AUTOLAB_TRAIN_PORT=21011

# 训推 GPU 互斥锁目录（默认 /var/run/niii-gpu{index}.lock）
# NIII_GPU_LOCK_DIR=/var/run
# NIII_GPU_INFER_TIMEOUT=120
