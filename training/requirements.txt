# 闭环训练环境（建议 mamba env: yolo，与 NIII_YOLO_PYTHON 一致）
# 从 niii_training_algorithm/requirements-yolo.txt 移植

ultralytics>=8.3.0,<8.4
PyYAML>=6.0
opencv-python-headless>=4.8
numpy>=1.24
pandas>=2.0
psutil>=5.9
tqdm>=4.60

# HTTP 入口（可选）
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0

# GPU 版 torch 请按 CUDA 驱动单独安装，例如：
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
